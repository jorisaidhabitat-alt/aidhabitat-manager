# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Vercel build (clones Flutter SDK into ./flutter on the build image)
/flutter/
.vercel/

# sqflite_common_ffi_web setup copies these here at build time — re-run
# `dart run sqflite_common_ffi_web:setup` to regenerate (also done by
# vercel-build.sh on every Vercel deploy).
/web/sqflite_sw.js
/web/sqlite3.wasm

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Vercel build (clones Flutter SDK into ./flutter on the build image)
/flutter/
.vercel/

# sqflite_common_ffi_web setup copies these at build time — re-run
# `dart run sqflite_common_ffi_web:setup` (also done in vercel-build.sh).
/web/sqflite_sw.js
/web/sqlite3.wasm
