// Credentials removed for security — authentication is handled server-side.

export const profilsAutorises = {};

export const nocoDbTokensParEmail = {};

export const LOCAL_SESSION_TOKEN_PREFIX = 'local-auth:';
